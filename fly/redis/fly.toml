# fly/redis/fly.toml — Application Redis séparée
app            = "mon-app-redis"
primary_region = "cdg"

[build]
  image = "redis:7.4-alpine"

[[mounts]]
  source      = "redis_data"
  destination = "/data"

[[vm]]
  memory   = "256mb"
  cpu_kind = "shared"
  cpus     = 1
